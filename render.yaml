services:
  - type: web
    name: wuxia-as-a-service
    env: java
    plan: free
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/app-service-0.0.1-SNAPSHOT.jar
    healthCheckPath: /
    envVars:
      - key: SERVER_PORT
        value: 10000
